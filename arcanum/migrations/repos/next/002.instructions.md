Retrofit `.claude/agents/architect.md` in the target repo to delegate
exploration and planning to the right specialist agent, not just
implementation — mirroring the updated `init-claude` `architect`
template.

1. Check whether `.claude/agents/architect.md` exists in the target
   repo. If it does not exist, there is nothing to do — mark this
   entry complete without making any changes.

2. In the file's "Specialist agents" section, find the delegation line
   (originally "Delegate implementation work to the right agent. Never
   implement what belongs to a specialist yourself.") and extend it to
   also cover exploration and planning:

   > Delegate implementation, exploration, and planning work to the
   > right agent. Never implement, explore, or plan what belongs to a
   > specialist yourself.

   If the line has been customized/reworded rather than left as the
   `init-claude` default, adapt the same broadening (add exploration
   and planning alongside implementation) to the file's actual current
   wording instead of overwriting it wholesale.

3. In the file's "How to coordinate" section, insert an additional
   step (before the "Integrate" step): "Delegate exploration first —
   before proposing an approach, dispatch the specialist(s) whose
   scope covers the relevant area to investigate, rather than reading
   the code yourself." Adapt to the section's existing numbering and
   wording (e.g. renumber subsequent steps).

4. If either section is missing, or has been restructured beyond
   recognition (so it's unclear where the equivalent guidance should
   go), ask the user where to place it rather than guessing.

5. Save the file.

This migration only ever touches `.claude/agents/architect.md`.
Specialist agent files (`.claude/agents/<agent-name>.md`) are never
touched — the `init-claude` template for those was not changed by this
migration.
