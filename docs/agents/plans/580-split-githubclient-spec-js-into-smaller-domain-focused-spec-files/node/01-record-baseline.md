# Record the baseline spec count
Before changing anything, run `yarn test` (or `npx jasmine`) in `core/` and note the total number of specs jasmine reports ("N specs, 0 failures"). Also note how many specs `GitHubClient_spec.js` contributes on its own (`npx jasmine core/spec/lib/utils/github/GitHubClient_spec.js`). Step 04 compares against both numbers.

## Files to Change
- (none; this step only records numbers)
