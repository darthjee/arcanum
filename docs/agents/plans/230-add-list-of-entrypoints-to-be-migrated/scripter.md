# scripter Plan: Add list of entrypoints to be migrated

Main plan: [plan.md](plan.md)

## Implementation Steps

### Step 1 — Add every pending entrypoint to `migration-status.json` with `false`
Keep the 4 existing `true` entries (`dispatch-fixture`, `dispatch-fixture-crash`, `resolve-and-fetch`, `resolve-id-and-file`) untouched. Add these new keys, all `false`:

**`arcanum/_lib/*.sh` (7 keys, unqualified — existing convention):**

| Key | Script |
| --- | --- |
| `github-issue` | `github_issue.sh` |
| `issue-state` | `issue_state.sh` |
| `permission-grant` | `permission_grant.sh` |
| `checkout-safe-branch` | `checkout_safe_branch.sh` |
| `list-agents` | `list_agents.sh` |
| `resolve-plan-paths` | `resolve_plan_paths.sh` |
| `spawn-issue` | `spawn_issue.sh` |

**Skill-level `<skill>/scripts/*.sh` (36 keys, qualified `<skill-name>-<script-basename>` to avoid collisions — `config.sh`/`github.sh` exist as distinct, non-identical implementations in more than one skill):**

| Skill | Keys to add |
| --- | --- |
| `arcanum-split-issue` | `arcanum-split-issue-create-sub-issue-file`, `arcanum-split-issue-create-sub-issue`, `arcanum-split-issue-finish`, `arcanum-split-issue-push-sub-issues` |
| `arcanum-update` | `arcanum-update-run-update` |
| `auto-fix-all` | `auto-fix-all-checkout-from-main`, `auto-fix-all-cleanup-artifacts`, `auto-fix-all-config`, `auto-fix-all-github`, `auto-fix-all-queue`, `auto-fix-all-reply-comment`, `auto-fix-all-wait-ci-and-merge`, `auto-fix-all-wait-ci` |
| `auto-fix-issue` | `auto-fix-issue-commit-change`, `auto-fix-issue-create-branch`, `auto-fix-issue-github`, `auto-fix-issue-list-plan-agents`, `auto-fix-issue-list-plan-steps`, `auto-fix-issue-merge-main`, `auto-fix-issue-run-checks` |
| `auto-monitor-issue-pr` | `auto-monitor-issue-pr-resolve-pr-number` |
| `auto-monitor-pr` | `auto-monitor-pr-monitor-pr` |
| `auto-new-issue` | `auto-new-issue-commit-issue` |
| `auto-plan-issue` | `auto-plan-issue-commit-plan` |
| `discuss-issue` | `discuss-issue-confirm`, `discuss-issue-render-issue` |
| `init-claude` | `init-claude-set-ci-ignored-patterns`, `init-claude-setup-docs-structure`, `init-claude-setup-templates`, `init-claude-stamp-arcanum-version`, `init-claude-sync-labels`, `init-claude-write-label-config` |
| `monitor-issues` | `monitor-issues-config`, `monitor-issues-github`, `monitor-issues-monitor-issues`, `monitor-issues-rewrite-queue` |

**Do NOT add a key for** (verified during `discuss-issue`, see the issue file's `## Solution` for the full reasoning):
- Sourced-only `arcanum/_lib/*.sh` utilities (guard pattern or explicit "meant to be SOURCED" comment): `agent_email.sh`, `commit_template.sh`, `config_chain.sh`, `git_branch.sh`, `global_config.sh`, `lock.sh`, `merge_body.sh`, `origin.sh`, `push.sh`, `repo_config.sh`, `repo_path.sh`, `tags.sh`, `tag_mutate.sh`, `safe_branch.sh`, `tag_actions.sh`.
- `engine_dispatch.sh` (the dispatch infrastructure itself), `resolve_and_fetch_shell.sh` / `resolve_id_and_file_shell.sh` (internal shell-implementation half of the already-tracked `resolve-and-fetch`/`resolve-id-and-file` entries).
- Thin `exec`-only skill-level wrappers that delegate their entire body to an already-tracked `arcanum/_lib` script (migrating the canonical script covers them): `arcanum-split-issue/scripts/github.sh`, `auto-new-issue/scripts/github.sh`, `discuss-issue/scripts/github.sh`, `enhance-issue/scripts/github.sh` (→ `github_issue.sh`); `auto-fix-issue/scripts/issue_state.sh` (→ `issue_state.sh`); `auto-fix-issue/scripts/resolve_plan_paths.sh`, `auto-plan-issue/scripts/resolve_plan_paths.sh` (→ `resolve_plan_paths.sh`); `auto-new-issue/scripts/resolve_id_and_file.sh`, `discuss-issue/scripts/resolve_id_and_file.sh` (→ `resolve_id_and_file.sh`); `discuss-issue/scripts/resolve_and_fetch.sh` (→ `resolve_and_fetch.sh`); `auto-plan-issue/scripts/list_agents.sh`, `discuss-issue/scripts/list_agents.sh`, `plan-issue/scripts/list_agents.sh` (→ `list_agents.sh`).
- `arcanum-split-issue/scripts/test_create_sub_issue_file.sh` (standalone regression test, not wired into any skill flow) and `init-claude/scripts/lib/label_config.sh` (sourced-only utility).

Validate the edited JSON parses with `jq . arcanum/_lib/migration-status.json` before moving on — no CI job currently checks this file's syntax.

### Step 2 — Regenerate the migration status table
Run `scripts/generate_entrypoint_migration_status.sh` from the repo root. It reads the updated JSON and rewrites `docs/agents/architecture/entrypoint-migration-status.md` in place — do not hand-edit that file, it's auto-generated. Confirm the new rows appear with `Migrated=No` and a blank `Issue` column (expected: no commit ever introduced `true` for a brand-new key).

## Files to Change
- `arcanum/_lib/migration-status.json` — add the ~43 keys above, all `false`.
- `docs/agents/architecture/entrypoint-migration-status.md` — regenerated by running `scripts/generate_entrypoint_migration_status.sh`, not hand-edited.

## Notes
- Naming convention: `arcanum/_lib/*.sh` keys stay unqualified kebab-case (existing precedent); skill-level keys are qualified `<skill-name>-<script-basename>` to avoid collisions between skills that independently implement a same-named script (`config.sh`, `github.sh`).
- Two `arcanum/_lib` entrypoints (`resolve-plan-paths`, `spawn-issue`) were missed by the original issue analysis and confirmed directly against the source during `discuss-issue` — verified as real entrypoints (own shebang/usage, called directly, not sourced).
- No CI job exercises `arcanum/_lib/*.sh` or skill `scripts/*.sh` directly (the `test`/`checks` CircleCI jobs only cover `core/`), so there's no automated check to catch a bad key or JSON syntax error here — rely on `jq` validation and a manual read of the regenerated table.
