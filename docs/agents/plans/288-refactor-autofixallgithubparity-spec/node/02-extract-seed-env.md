# Extract seedEnv

Create `core/spec/support/utils/parityEnv.js` and move `seedEnv` (currently `core/spec/bin/autoFixAllGithubParity_spec.js` lines 101–116) into it, exported, with its JSDoc comment adapted from the original.

## Files to Change

- `core/spec/support/utils/parityEnv.js` — new file: `seedEnv`, exported.
