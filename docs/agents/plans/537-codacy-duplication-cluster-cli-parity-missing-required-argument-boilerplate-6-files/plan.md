# Plan: Codacy: duplication cluster — CLI parity "missing required argument" boilerplate (6 files)

Issue: [537-codacy-duplication-cluster-cli-parity-missing-required-argument-boilerplate-6-files.md](../../issues/537-codacy-duplication-cluster-cli-parity-missing-required-argument-boilerplate-6-files.md)

## Overview

This is a `core/spec/` test-only refactor, entirely within the `node` agent's scope. See [node.md](node.md) for the full plan.
