# architect Plan: Codacy: markdownlint MD060 cluster — inconsistent table column style (26 findings, 15 files)

Main plan: [plan.md](plan.md)

## Shared contracts

Reformat every table below to `compact` style: single space of padding around each cell's content, delimiter row not stretched to match column width. Do not change cell content, add/remove columns, or reorder rows. This same rule is applied independently by `skill-writer` to its own files.

## Implementation Steps

### Step 1 — Pin the table style in `.markdownlint.json`

Add `"MD060": { "style": "compact" }` to `.markdownlint.json`, mirroring how `"MD013": false` was pinned in #492/#493.

### Step 2 — Reformat the 9 hand-written tables, regenerate the 2 autogenerated docs

For each hand-written file below, reformat the table at the given line(s) to `compact` style. For the 2 autogenerated files, do not hand-edit — re-run their generator script instead, then verify the regenerated table is already `compact` (if the generator itself emits padded/aligned pipes, it needs a matching one-line fix so future regenerations stay compliant).

## Files to Change

- `.markdownlint.json` — add the `MD060` key pinning `style: "compact"`.
- `.claude/agents/architect.md:19` and `:49` — 2 tables to reformat.
- `AGENTS.md:23` and `:52` — 2 tables to reformat.
- `README.md:23` and `:130` — 2 tables to reformat.
- `docs/agents/architecture.md:6` — 1 table to reformat.
- `docs/agents/architecture/agent-roster-and-delegation.md:8` — 1 table to reformat.
- `docs/agents/architecture/dispatch-permissions.md:36` — 1 table to reformat.
- `docs/agents/architecture/shared-state-and-configuration.md:6` — 1 table to reformat.
- `docs/agents/folder-structure.md:6` — 1 table to reformat.
- `docs/guides/arcanum-repo-config.md:16` and `:58` — 2 tables to reformat.
- `docs/agents/architecture/entrypoint-migration-status.md` — AUTO-GENERATED; run `scripts/generate_entrypoint_migration_status.sh` to refresh, do not hand-edit.
- `docs/agents/tag-mutations.md` — AUTO-GENERATED; run `scripts/generate_tags_table.sh` to refresh, do not hand-edit.

## Notes

- If either generator script (`scripts/generate_entrypoint_migration_status.sh`, `scripts/generate_tags_table.sh`) emits a non-`compact` table (e.g. padded/aligned), fix the script's table-building logic so its output is `compact` by construction — a one-off hand-fix of the generated file would just be overwritten on the next regen.
- Re-run markdownlint (or Codacy) after both agents finish to confirm zero MD060 findings remain across all 15 files.
